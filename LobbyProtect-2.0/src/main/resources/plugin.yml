name: LobbyProtect
version: '${project.version}'
main: lobbyprotect.Main
api-version: "1.21"
commands:
  lpbld:
    permission: build.buildCommand
  lpdmg:
    permission: damage.damageCommand
  lpmob:
    permission: lpmob.control